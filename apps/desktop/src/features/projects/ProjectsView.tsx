import { PageContainer, ComingSoon } from "@/components/layout/PageContainer";

export function ProjectsView() {
  return (
    <PageContainer title="Projects" subtitle="Spaces where Miori keeps long-running work.">
      <ComingSoon note="Project workspaces will live here — briefs, milestones, and context Miori holds for you." />
    </PageContainer>
  );
}
